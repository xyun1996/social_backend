# 05 Ops Expansion

- Status: `completed`
- Version: `v2`

## Delivered

- richer player social reads
- queue expiry visibility in operator views
- worker retry/backoff visibility in operator views
- stronger aggregated runtime support surfaces on top of the `v1` baseline

## Acceptance

Operators can now inspect deeper social, queue, worker, and guild state than `v1` exposed, without turning ops support into a separate product track.
